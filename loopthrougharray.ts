let list:Array<any> = ["hello",'hi',20,'hola'];  // Array<Elemtype> is a generic array type.

for(let mylist of list)  // Syntax for looping through the array in typescript.
{
   console.log(mylist);
}