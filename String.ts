let fullname:string = 'Kevin Peterson';
let age:number =37;
let details:any = `Full name is ${fullname} and his age is ${age}`;  // $ {} denotes template strings which span multiple lines and embedded                                                                               expressions that surrounded by backtick/backquote (``)

console.log(details);